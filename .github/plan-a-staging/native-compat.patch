--- a/Type4MeIntelliSenseCore/IntelliSensePrompt.swift
+++ b/Type4MeIntelliSenseCore/IntelliSensePrompt.swift
@@ -309,10 +309,18 @@
         }
         // Stable ties preserve legacy ordering. No invented phonetic aliases,
         // recency shuffle, history scan, retrieval service or extra LLM call.
-        let ordered = vocabulary.enumerated().map {
-            (index: $0.offset, term: $0.element, rank: priority($0.element))
-        }.sorted {
-            $0.rank == $1.rank ? $0.index < $1.index : $0.rank > $1.rank
+        struct RankedTerm {
+            let index: Int
+            let term: String
+            let rank: Int
+        }
+        var ordered: [RankedTerm] = []
+        for (index, term) in vocabulary.enumerated() {
+            ordered.append(RankedTerm(index: index, term: term, rank: priority(term)))
+        }
+        ordered.sort { (lhs: RankedTerm, rhs: RankedTerm) -> Bool in
+            if lhs.rank == rhs.rank { return lhs.index < rhs.index }
+            return lhs.rank > rhs.rank
         }
         for item in ordered {
             let index = item.index, rawTerm = item.term
--- a/Type4Me/Services/HotwordStorage.swift
+++ b/Type4Me/Services/HotwordStorage.swift
@@ -128,7 +128,12 @@
 
     /// Persist user hotwords and surface file-system failures to callers that
     /// need all-or-nothing behavior (for example correction learning).
-    static func saveOrThrow(_ words: [String], notify: Bool = true) throws {
+    static func saveOrThrow(_ words: [String]) throws {
+        try saveOrThrow(words, notify: true)
+    }
+
+    /// The explicit overload keeps existing function references source-compatible.
+    static func saveOrThrow(_ words: [String], notify: Bool) throws {
         try writeFileOrThrow(words, to: userFileURL)
         cacheLock.lock()
         cachedUser = nil
--- a/Type4Me/UI/Settings/SmartCorrectionSheet.swift
+++ b/Type4Me/UI/Settings/SmartCorrectionSheet.swift
@@ -62,8 +62,10 @@
     }
 
     private var selectedCount: Int {
-        (learningScope == .hotwordAndMapping
-            ? snippetSuggestions.filter { $0.isSelected && !$0.isDuplicate }.count : 1)
+        // The explicitly entered pair is always part of this confirmation;
+        // no generated alias has to be selected to save that pair.
+        1 + (learningScope == .hotwordAndMapping
+            ? snippetSuggestions.filter { $0.isSelected && !$0.isDuplicate }.count : 0)
         + hotwordSuggestions.filter { $0.isSelected && !$0.isDuplicate }.count
     }
 
